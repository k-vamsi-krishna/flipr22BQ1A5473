# Backend Environment Configuration - Copy to .env and update values

NODE_ENV=production
PORT=5000
DATABASE_PATH=./database.db
CORS_ORIGIN=http://localhost:3000,http://localhost:5173

# Admin credentials (optional, for authentication if needed)
ADMIN_EMAIL=admin@realtrust.com
ADMIN_PASSWORD=changeme_in_production
